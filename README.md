````markdown
# 📡 Understanding Fetch API

A simple project using the **Fetch API** to `GET` and `POST` data from [JSONPlaceholder](https://jsonplaceholder.typicode.com/). Includes practical lessons and a dynamic web UI to fetch data with filters.

---

## 📘 Lessons Overview

### 1. Basic Fetch Syntax
- **File:** `lesson1.js`  
- Fetches a single post and logs it to the console.

### 2. GET Request with Error Handling
- **File:** `lesson2.js`  
- Demonstrates how to handle HTTP response errors.

### 3. POST Request
- **File:** `lesson3.js`  
- Sends form data using `fetch()` and logs the response.

### 4. GET with Query Parameters
- **File:** `lesson4.js`  
- Dynamically builds a URL using `URLSearchParams`.

### 5. Dynamic Web App
- **Files:** `index.html` + `api.js`  
- Interactive UI that allows users to:
  - Select a resource (`posts`, `comments`, etc.)
  - Enter filter values
  - Fetch and display results dynamically


## 🚀 How to Clone and Run

```bash
git clone https://github.com/Onesmus123/Fetch-API.git
cd Fetch-API
````

### To run lessons:

Open each `.js` file in a browser environment using Node.js setup (if configured).

### To run the Dynamic Web App:

1. Open `index.html` in your browser.
2. Select a resource and enter filter values.
3. Click **"Fetch Data"** to view results.


**Built by Onesmus Mutyauvyu David** ✨

```